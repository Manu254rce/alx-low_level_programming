# include "main.h"

/**
 * _isupper- Prints the uppercase letter
 * @c: integer character
 * Return: Always 0.
 */

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
