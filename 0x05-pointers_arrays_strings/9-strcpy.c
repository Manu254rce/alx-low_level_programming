# include "main.h"
# include <string.h>

/**
 * *_strcpy- Program function to copy a string pointer
 * @dest: variable to store copied string
 * @src: variable to store source srting
 * Return: Always 0
 */

char *_strcpy(char *dest, char *src)
{
	char *strcpy(...*dest, ...*src);

	return (*dest);
}
